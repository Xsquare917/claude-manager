# 更新日志

## v1.3.1 (2026-08-11)

### 重要变更

- **会话持久化与休眠恢复**
  - 会话元数据持久化到 `~/.claude-manager/state.json`，重启应用后会话列表完整恢复
  - 恢复的会话以休眠态展示（💤），点击时才拉起进程，通过 `claude --resume` 接续原对话
  - 终端进程退出（exit/崩溃）转为休眠而非删除，对话可随时找回
- **基于 Claude Code hooks 的精确状态检测**
  - 通过 `--settings` 注入 hooks 配置，状态由 UserPromptSubmit/PreToolUse/Notification/Stop 事件上报
  - 权限确认等待（原先无法检测）现在可以准确识别并标记未读
  - 移除整个终端输出嗅探层（spinner 字符匹配、防抖定时器等）
- **移除 AI 摘要残留代码**，删除 `@anthropic-ai/sdk` 依赖

### 新增

- 会话标题从 transcript 首条用户消息提取（逐键输入亦可，修复原标题提取失效问题）
- 侧边栏显示会话最后一条消息预览和上下文 token 量
- 系统通知：会话等待确认/任务完成时弹出，点击跳转对应会话（设置中可关）
- 终端 URL 悬停弹出磨砂玻璃操作卡（打开/复制），支持纯文本 URL 与 OSC 8 超链接
- 项目分组新增"+ 新会话"按钮，免去重复选择目录

### 优化

- 终端实例常驻：切换会话零重放、零闪烁，内容与滚动位置完整保留
- 终端区域恒定深色，保证 CLI 原始配色可读（应用 UI 主题不受影响）
- 目录选择器与 CLI 检测改为异步执行，不再阻塞终端输入输出
- CLI 检测并行化，启动检测提速

### 安全

- 服务只绑定 127.0.0.1，不再暴露到局域网
- 增加 Host 校验与 Socket.IO Origin 白名单，阻断恶意网页对本地终端的跨源访问

### 修复

- 快捷键修饰键精确匹配，不含修饰键的快捷键不再吞掉组合键
- 修复 socket 先于界面挂载连接时存量会话丢失的竞态
- 引导页版本标记收敛为单一常量

## v1.2.7 (2026-03-17)

### 新增
- 添加项目时弹出启动命令选择窗口
- 支持选择 Claude、Codex、Gemini 三种 CLI 工具
- 启动命令选择窗口显示命令输入框,支持手动编辑
- 设置页面和启动命令窗口的按钮根据 CLI 安装状态显示颜色(绿色=已安装,黄色=未安装)
- 新增 `/api/check-clis` 接口检测多个 CLI 工具的安装状态

### 优化
- 改进启动命令配置流程,更加灵活和直观

## v1.2.6 (2026-03-11)

### 重要变更

- **移除对话概括功能**
  - 移除"批量刷新概括"按钮
  - 移除 AI 概括生成相关代码
  - 解决因 API Key 未配置导致的概括生成失败问题

- **会话标题自动提取**
  - 会话标题自动从用户第一次输入中提取（前6个字）
  - 不再显示"新会话"，而是显示实际输入内容

### 新功能

- **启动命令快捷按钮**
  - 设置页启动命令输入框下方添加三个快捷按钮
  - 支持一键填入 Claude、Codex、Gemini 命令
  - 深色/浅色主题适配

### 优化

- **内存优化**
  - 缓冲区大小从 5000 降至 2500，减少约 50% 内存占用

- **UI 优化**
  - 修复设置模态框高度问题，添加最大高度限制（85vh）
  - 设置内容区域支持滚动，适配小窗口

---

## v1.2.5 (2025-02-04)

### 新功能

- **启动命令配置**
  - 设置中添加"启动命令"配置项
  - 支持自定义 claude 启动参数（如 `claude --skip-permissions`）
  - 新建会话时自动使用配置的命令

- **自动下载安装更新**
  - 检测到新版本后显示"下载安装"按钮
  - 自动识别当前平台，下载对应安装包（.dmg / .exe）
  - 显示下载进度，完成后自动打开安装包

### 优化

- **未读提示红点优化**
  - 红点尺寸增大（8px → 10px）
  - 添加缓慢闪烁动画，更加醒目
  - 添加发光效果

---

## v1.2.2 (2025-01-30)

### 新功能

- **设置页面添加检测更新按钮**
  - 在"关于"区域的版本号左侧添加"检测更新"按钮
  - 点击后显示检测结果：发现新版本/已是最新/检测失败
  - 发现新版本时提供下载链接

### 修复

- **修复光标位置与输入不同步问题**
  - 切换会话时多次 resize 同步光标位置（50ms、150ms、300ms）
  - 确保 xterm 光标与 PTY 实际光标位置一致

- **修复侧边栏按钮点击导致终端失焦问题**
  - 所有侧边栏按钮添加 `onMouseDown` 阻止默认焦点转移
  - 包括：设置、添加项目、删除项目、删除会话、批量刷新概括

- **修复批量刷新概括功能**
  - 修复空会话时未调用 `resolve()` 导致队列卡住的问题
  - 修复错误处理时未更新 sessionManager 状态的问题
  - 按钮禁用时间改为动态计算（根据会话数量）

- **修复浅色主题下未读红点不显示问题**
  - 为浅色主题添加更深的红点颜色和阴影效果

- **修复浅色主题下代码块文字不可见问题**
  - 将 ANSI white/brightWhite 颜色改为深灰色
  - 确保代码块内容在白色背景下清晰可见

- **修复会话历史记录显示不完整问题**
  - 将历史缓冲区从 1000 增加到 5000 个数据块
  - 支持存储更长的会话历史

### 优化

- **浅色主题光标颜色优化**
  - 光标改为淡紫色，更柔和美观

- **浅色主题终端黑色文字优化**
  - 黑色改为中灰色，避免对比度过高

- **忙碌状态颜色优化**
  - 从黄色改为橙色，更符合"活跃/进行中"的语义

---

## v1.2.1 (2025-01-30)

### 修复

- **修复退出时会话未清理问题**
  - 应用退出时自动 kill 所有 Claude 会话进程
  - 避免关闭应用后留下孤儿进程

- **修复光标位置错乱问题**
  - 切换会话时光标可能不在输入行的问题
  - 历史加载后自动滚动到底部
  - 重新同步终端尺寸，确保 CLI 正确绘制输入行

- **修复常用指令模板不生效问题**
  - 设置页添加的指令模板现在能正确填入新会话
  - 等待终端初始化完成后再填入模板内容

- **修复浅色主题终端内容不可见问题**
  - 配置完整的 ANSI 16 色调色板
  - 将白色系文字改为深色，确保在浅色背景下可见
  - 优化黄色系颜色，避免太浅看不清

- **优化摘要生成功能**
  - 增加 API 请求间隔（500ms → 1500ms），减少限流
  - 添加自动重试机制（最多 2 次）
  - 清理终端控制字符，提高摘要质量
  - 更详细的错误提示（区分限流、认证、解析错误）

### 改进

- **简化添加项目流程**
  - 点击添加项目直接打开目录选择器
  - 移除中间的模态框步骤

- **优化终端初始化**
  - PTY 默认尺寸调整为标准 80x24
  - 减少初始尺寸与实际尺寸的差异

---

## v1.2.0

- 初始发布版本
- 多项目管理
- 多会话支持
- AI 状态摘要
- 主题切换
- 自动更新检查
